{
  "version": 1,
  "ports": {"preview": 3000, "additional": {}},
  "scripts": {
    "setup": {"enabled": true, "command": "command -v git-lfs >/dev/null 2>&1 || sudo apt-get install -y git-lfs >/dev/null 2>&1; npm install --no-audit --no-fund"},
    "run": {"enabled": true, "command": "npm run dev"},
    "archive": {"enabled": true, "command": null},
    "check": {"enabled": true, "command": "npm run build"}
  },
  "mcpServers": []
}
